# homebrew-brewfile

Brewfiles

Linux and MacOS flavors

Notibly the Linux Brewfile does not contain any casks as cask don't work on Linux.

## Running

For MacOS

```shell
brew bundle --file=macos/Brewfile
```
